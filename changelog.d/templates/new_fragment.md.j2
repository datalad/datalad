<!-- Uncomment the section that is right (remove the HTML comment wrapper).

     Keep the entry short and scaled to the change: a small fix needs one
     sentence, a large feature a summary plus a pointer to the docs.  Rule of
     thumb ~40 words.  Summarize the cause where it helps the reader, but
     leave its details and the diff to the commit message and the PR.
     See "Writing the entry" in CONTRIBUTING.md. -->
{% for cat in config.categories -%}
<!--
### {{ cat }}

- Describe change, possibly reference closed/related issue/PR.
  Fixes [#XXX](https://github.com/datalad/datalad/issues/XXX) via
  [PR #YYY](https://github.com/datalad/datalad/pull/YYY)
  (by [@GITHUBHANDLE](https://github.com/GITHUBHANDLE))
-->
{% endfor -%}
