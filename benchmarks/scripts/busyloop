#!/usr/bin/env python

from time import time
import sys

total = float(sys.argv[1])
t0 = time()

target_time = t0 + total
t = t0
while t < target_time:
    print("I am looping already for {} seconds".format(t - t0))
    t = time()
